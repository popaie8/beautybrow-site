---
const flowSteps = {
  lash: [
    { step: 1, title: 'カウンセリング', description: 'お悩みや理想の仕上がりをお伺いします' },
    { step: 2, title: 'ロッド選定', description: '目の形に合わせて最適なロッドを選びます' },
    { step: 3, title: '施術', description: 'まつ毛を1本1本丁寧に立ち上げます' },
    { step: 4, title: 'アフターケア', description: 'ホームケアの方法をご説明します' },
  ],
  brow: [
    { step: 1, title: 'カウンセリング', description: '骨格や毛流れを分析します' },
    { step: 2, title: 'デザイン設計', description: '黄金比を基にデザインを描きます' },
    { step: 3, title: '施術', description: 'パーマ液で眉毛を立ち上げます' },
    { step: 4, title: '仕上げ', description: 'メイクで仕上がりを確認します' },
  ],
};
---

<section class="flow section bg-muted">
  <div class="container">
    <div class="section-header">
      <span class="section-label">FLOW</span>
      <h2 class="section-title">施術の流れ</h2>
      <p class="section-lead">初めての方でも安心。丁寧にご案内します</p>
    </div>
    
    <div class="flow-container">
      <div class="flow-category">
        <h3 class="flow-category-title">
          <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
            <path d="M2 12s3-7 10-7 10 7 10 7-3 7-10 7-10-7-10-7z"></path>
            <circle cx="12" cy="12" r="3"></circle>
          </svg>
          まつ毛パーマの流れ
        </h3>
        <div class="flow-steps">
          {flowSteps.lash.map((step) => (
            <div class="flow-step">
              <div class="flow-step-number">STEP {step.step}</div>
              <h4 class="flow-step-title">{step.title}</h4>
              <p class="flow-step-description">{step.description}</p>
            </div>
          ))}
        </div>
      </div>
      
      <div class="flow-category">
        <h3 class="flow-category-title">
          <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
            <path d="M2 12s3-7 10-7 10 7 10 7-3 7-10 7-10-7-10-7z"></path>
            <circle cx="12" cy="12" r="3"></circle>
          </svg>
          眉パーマの流れ
        </h3>
        <div class="flow-steps">
          {flowSteps.brow.map((step) => (
            <div class="flow-step">
              <div class="flow-step-number">STEP {step.step}</div>
              <h4 class="flow-step-title">{step.title}</h4>
              <p class="flow-step-description">{step.description}</p>
            </div>
          ))}
        </div>
      </div>
    </div>
    
    <div class="flow-note">
      <p>※初回はカウンセリングを含めて15分程度お時間をいただきます</p>
    </div>
  </div>
</section>

<style>
  .flow {
    background: var(--color-bg-muted);
  }
  
  .container {
    max-width: var(--container-width, 1120px);
    margin: 0 auto;
    padding: 0 2rem;
  }
  
  .section-header {
    text-align: center;
    margin-bottom: 3rem;
  }
  
  .section-label {
    display: inline-block;
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.2em;
    color: var(--color-accent);
    margin-bottom: 1rem;
  }
  
  .section-title {
    font-family: var(--font-serif);
    font-size: clamp(1.75rem, 4vw, 2.5rem);
    font-weight: 600;
    color: var(--color-text);
    margin-bottom: 0.5rem;
    line-height: 1.4;
  }
  
  .section-lead {
    font-size: 1rem;
    color: var(--color-text-muted);
    line-height: 1.8;
  }
  
  .flow-container {
    display: flex;
    flex-direction: column;
    gap: 3rem;
  }
  
  .flow-category-title {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    font-size: 1.25rem;
    font-weight: 600;
    color: var(--color-text);
    margin-bottom: 2rem;
  }
  
  .flow-category-title svg {
    color: var(--color-accent);
  }
  
  .flow-steps {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1.5rem;
  }
  
  .flow-step {
    position: relative;
    padding: 1.5rem;
    background: white;
    border-radius: 1rem;
    text-align: center;
  }
  
  .flow-step::after {
    content: '';
    position: absolute;
    top: 50%;
    right: -0.75rem;
    transform: translateY(-50%);
    width: 1.5rem;
    height: 2px;
    background: var(--color-accent);
    opacity: 0.3;
  }
  
  .flow-step:last-child::after {
    display: none;
  }
  
  .flow-step-number {
    display: inline-block;
    padding: 0.25rem 0.75rem;
    background: var(--color-accent);
    color: white;
    font-size: 0.75rem;
    font-weight: 600;
    border-radius: 50px;
    margin-bottom: 1rem;
  }
  
  .flow-step-title {
    font-size: 1rem;
    font-weight: 600;
    color: var(--color-text);
    margin-bottom: 0.5rem;
  }
  
  .flow-step-description {
    font-size: 0.875rem;
    color: var(--color-text-muted);
    line-height: 1.6;
  }
  
  .flow-note {
    text-align: center;
    margin-top: 2rem;
    padding: 1rem;
    background: white;
    border-radius: 0.5rem;
  }
  
  .flow-note p {
    font-size: 0.875rem;
    color: var(--color-text-muted);
    margin: 0;
  }
  
  @media (max-width: 768px) {
    .container {
      padding: 0 1rem;
    }
    
    .flow-steps {
      grid-template-columns: 1fr;
      gap: 1rem;
    }
    
    .flow-step::after {
      display: none;
    }
    
    .flow-step {
      padding: 1rem;
    }
  }
</style>